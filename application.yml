eureka:
  client:
    service-url:
      defaultZone: http://${holiday-eureka-host:localhost}:8761/eureka

spring:
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://172.80.2.1/:9411/
    sender:
      type: rabbit
  rabbitmq:
    host: 172.80.2.1